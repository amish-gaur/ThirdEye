# SafeWatch — action router (Person 2)
# Targets Python 3.11+. No vision deps here; this branch is pure orchestration.

# External APIs
anthropic>=0.40.0
elevenlabs>=1.8.0
twilio>=9.3.0

# HTTP service (receives events from the vision pipeline)
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0
requests>=2.32

# Dev / tests
pytest>=8.0
pytest-mock>=3.14
httpx>=0.27        # required by fastapi.testclient.TestClient
