# SafeWatch — combined backend branch
# Targets Python 3.11+. Includes action-router + vision-pipeline deps.

# External APIs
anthropic>=0.40.0
elevenlabs>=1.8.0
twilio>=9.3.0

# HTTP service (receives events from the vision pipeline)
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0
requests>=2.32
websockets>=12.0   # phone-camera WebSocket ingest (used by FastAPI/uvicorn)
qrcode>=7.4        # QR pairing UI (renders PNGs via Pillow)
zeroconf>=0.131    # mDNS LAN camera discovery + safewatch_sensor announce

# Vision pipeline
opencv-python>=4.10
torch>=2.5
torchvision>=0.20
transformers>=4.55
ultralytics>=8.3
pillow>=10.4
lap>=0.5.12        # Ultralytics ByteTrack linear assignment (auto-installed otherwise)

# Cross-camera ReID + MCP demo
torchreid>=0.2.5   # OSNet person re-identification
tensorboard>=2.20  # transitive dep of torchreid (imported at module load)
mcp>=1.27          # MCP SDK for the Claude.ai-as-UI demo surface
gdown>=6.0         # download ReID weights from Google Drive model zoo

# Face exclusion (enrolled family members never trigger Qwen)
insightface>=0.7
onnxruntime>=1.17

# Dev / tests
pytest>=8.0
pytest-mock>=3.14
httpx>=0.27        # required by fastapi.testclient.TestClient
